# Architecture Decisions

> This file holds pointers, never facts. If a statement here could go out of date, it belongs in
> its source document instead. Deleting this entire directory must lose no information.

## Purpose

A pointer to the ADR index, so a reader knows where decisions are recorded. This file is an index
of an index — it never restates a decision's Context, Decision, or Consequences; that would
duplicate `docs/adr/` and drift from it the moment either changed.

## Source of truth

[`docs/adr/README.md`](../adr/README.md) (index only; never restate a decision).

## Accepted

TODO: point to `docs/adr/README.md`'s index, filtered to Accepted — not a restated list.

## Proposed

TODO: point to `docs/adr/README.md`'s index, filtered to Proposed — not a restated list.

## Superseded

TODO: point to `docs/adr/README.md`'s index, filtered to Superseded — not a restated list.
