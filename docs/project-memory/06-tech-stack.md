# Tech Stack

> This file holds pointers, never facts. If a statement here could go out of date, it belongs in
> its source document instead. Deleting this entire directory must lose no information.

## Purpose

A pointer to where the project's technology choices are recorded, without restating versions or
package names here — those live in files that are mechanically kept accurate; this one isn't.

## Source of truth

`package.json`; [`docs/02-architecture.md`](../02-architecture.md); ADR 0001.

## Frontend

TODO: point to `package.json` / ADR 0001, not a restated version.

## Backend

TODO: point to `package.json` / ADR 0001, not a restated version.

## Infrastructure

TODO: point to `docs/02-architecture.md`, not a restated detail.

## Tooling

TODO: point to `package.json` / `docs/02-architecture.md`, not a restated list.
