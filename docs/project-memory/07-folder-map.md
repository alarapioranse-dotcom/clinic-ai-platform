# Folder Map

> This file holds pointers, never facts. If a statement here could go out of date, it belongs in
> its source document instead. Deleting this entire directory must lose no information.

## Purpose

A pointer to how to read the repository's own folder structure, without restating that structure
here — the tree itself is always more current than a copy of it would be.

## Source of truth

The repository tree itself.

## Top-Level Folders

TODO: point to how to list the repository root, not a restated tree.

## Key Files

TODO: point to how to find key files (e.g. the repository's own listing), not a restated list.
