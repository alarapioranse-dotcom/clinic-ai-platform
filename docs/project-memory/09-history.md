# History

> This file holds pointers, never facts. If a statement here could go out of date, it belongs in
> its source document instead. Deleting this entire directory must lose no information.

## Purpose

A pointer to how to find the project's history, without maintaining a second, hand-written copy
of it that can drift from the real record.

## Source of truth

Git log and merged pull requests; never a hand-maintained narrative.

## Timeline

TODO: point to how to read the git log / merged PR list, not a restated timeline.
